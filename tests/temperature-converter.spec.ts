import { test, expect } from "@playwright/test";

// test url: https://seven-guis.netlify.app/temperature-converter
test("can convert from Celsius to Fahrenheit and vice versa", async ({
  page,
}) => {});
